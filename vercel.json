{
    "buildCommand": "cd client && npm config set registry https://registry.npmjs.org/ && npm i && npm run build",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "/api/index.js"
      }
    ]
  }